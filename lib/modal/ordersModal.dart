class OrdersModal {
  String? orderedMealName, orderedMealType, orderedMealDate, orderedMealPrice;

  OrdersModal(
      {this.orderedMealDate,
      this.orderedMealName,
      this.orderedMealPrice,
      this.orderedMealType});
}
